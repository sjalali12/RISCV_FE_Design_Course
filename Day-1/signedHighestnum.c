#include <stdio.h>
#include <math.h>

int main(){
	long long int max=(long long int)(pow(2,10) -1);
	long long int min=(long long int)(pow(2,10)* -1);
	printf("highest number represented by long long int max is %lld\n", max);
	printf("highest number represented by long long int min is %lld\n", min);
	return 0;
}
